// Unit tests for skiplist.c — leaderboard ordered by (score, id).
#include "macminidb.h"
#include <stdio.h>

int main(void) {
  // TODO: insert ordering, update re-sorts, topN, rank is 1-based, remove.
  printf("PASS skiplist_placeholder\n");
  return 0;
}
